import React from 'react'

const Demo<%= (rootOptions.plugins && rootOptions.plugins['@vue/cli-plugin-typescript']) ? `: React.FC` : `` %> = () => (
  <div className="App">
    <header className="App-header">
      <h3>Demo page</h3>
    </header>
  </div>
)

export default Demo
